name: Basic CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Show Current Directory
        run: pwd